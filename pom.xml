<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <parent>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-starter-parent</artifactId>
    <version>3.5.16</version>
    <relativePath/>
  </parent>

  <groupId>tr.ebrar</groupId>
  <artifactId>talep-onay</artifactId>
  <version>0.1.0-SNAPSHOT</version>
  <name>talep-onay</name>
  <description>Kurumsal talep ve onay yonetim sistemi</description>

  <properties>
    <java.version>21</java.version>
    <springdoc.version>2.9.0</springdoc.version>
    <jjwt.version>0.13.0</jjwt.version>
    <jacoco.version>0.8.15</jacoco.version>
    <archunit.version>1.5.0</archunit.version>
    <pitest.version>1.25.9</pitest.version>
    <pitest.junit5.version>1.2.3</pitest.junit5.version>
    <spotless.version>3.10.0</spotless.version>
    <!-- ./mvnw flyway:migrate icin varsayilan yerel baglanti; -Ddb.url=... ile gecilebilir -->
    <db.url>jdbc:postgresql://localhost:5432/talep_onay</db.url>
    <db.user>talep</db.user>
    <db.password>talep</db.password>
  </properties>

  <dependencies>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-web</artifactId>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-data-jpa</artifactId>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-security</artifactId>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-validation</artifactId>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-actuator</artifactId>
    </dependency>

    <dependency>
      <groupId>org.flywaydb</groupId>
      <artifactId>flyway-core</artifactId>
    </dependency>
    <dependency>
      <groupId>org.flywaydb</groupId>
      <artifactId>flyway-database-postgresql</artifactId>
    </dependency>
    <dependency>
      <groupId>org.postgresql</groupId>
      <artifactId>postgresql</artifactId>
      <scope>runtime</scope>
    </dependency>

    <dependency>
      <groupId>org.springdoc</groupId>
      <artifactId>springdoc-openapi-starter-webmvc-ui</artifactId>
      <version>${springdoc.version}</version>
    </dependency>

    <dependency>
      <groupId>io.jsonwebtoken</groupId>
      <artifactId>jjwt-api</artifactId>
      <version>${jjwt.version}</version>
    </dependency>
    <dependency>
      <groupId>io.jsonwebtoken</groupId>
      <artifactId>jjwt-impl</artifactId>
      <version>${jjwt.version}</version>
      <scope>runtime</scope>
    </dependency>
    <dependency>
      <groupId>io.jsonwebtoken</groupId>
      <artifactId>jjwt-jackson</artifactId>
      <version>${jjwt.version}</version>
      <scope>runtime</scope>
    </dependency>

    <dependency>
      <groupId>org.projectlombok</groupId>
      <artifactId>lombok</artifactId>
      <optional>true</optional>
    </dependency>

    <!-- Giris denemesi sayaci icin: suresi dolan kayitlari kendisi temizliyor ve
         boyutu sinirli. Elle yazilan bir Map + zamanlayici ile ayni isi yapmak
         mumkun ama bellek sizintisi riskini bedava satin almak olurdu.
         Surumu Spring Boot yonetiyor. -->
    <dependency>
      <groupId>com.github.ben-manes.caffeine</groupId>
      <artifactId>caffeine</artifactId>
    </dependency>

    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-test</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.springframework.security</groupId>
      <artifactId>spring-security-test</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-testcontainers</artifactId>
      <scope>test</scope>
    </dependency>
    <!-- Commit sonrasi calisan dinleyiciyi beklemek icin; Thread.sleep yazmaktan iyi -->
    <dependency>
      <groupId>org.awaitility</groupId>
      <artifactId>awaitility</artifactId>
      <scope>test</scope>
    </dependency>
    <!-- Mimari kurallari test olarak yazmak icin: katman ihlalleri derlemede degil
             testte yakalanir ve gerekce koda yakin durur -->
    <dependency>
      <groupId>com.tngtech.archunit</groupId>
      <artifactId>archunit-junit5</artifactId>
      <version>${archunit.version}</version>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.testcontainers</groupId>
      <artifactId>junit-jupiter</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.testcontainers</groupId>
      <artifactId>postgresql</artifactId>
      <scope>test</scope>
    </dependency>
  </dependencies>

  <build>
    <plugins>
      <plugin>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-maven-plugin</artifactId>
        <configuration>
          <excludes>
            <exclude>
              <groupId>org.projectlombok</groupId>
              <artifactId>lombok</artifactId>
            </exclude>
          </excludes>
        </configuration>
      </plugin>

      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-compiler-plugin</artifactId>
        <configuration>
          <annotationProcessorPaths>
            <path>
              <groupId>org.projectlombok</groupId>
              <artifactId>lombok</artifactId>
              <version>${lombok.version}</version>
            </path>
          </annotationProcessorPaths>
        </configuration>
      </plugin>

      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-surefire-plugin</artifactId>
        <configuration>
          <!-- Varsayilan sira "filesystem": yerelde ve CI'da farkli calisiyor,
                         bu yuzden bir sira bagimliligi yerelde gorunmeden CI'da patliyor.
                         Alfabetik sabitleyerek hatayi tekrar uretilebilir yaptik.
                         Testlerin siradan bagimsiz olmasi yine de sart; bkz. decisions K-013. -->
          <runOrder>alphabetical</runOrder>
        </configuration>
      </plugin>

      <plugin>
        <groupId>org.flywaydb</groupId>
        <artifactId>flyway-maven-plugin</artifactId>
        <configuration>
          <url>${db.url}</url>
          <user>${db.user}</user>
          <password>${db.password}</password>
          <locations>
            <location>filesystem:src/main/resources/db/migration</location>
          </locations>
        </configuration>
        <dependencies>
          <dependency>
            <groupId>org.flywaydb</groupId>
            <artifactId>flyway-database-postgresql</artifactId>
            <version>${flyway.version}</version>
          </dependency>
          <dependency>
            <groupId>org.postgresql</groupId>
            <artifactId>postgresql</artifactId>
            <version>${postgresql.version}</version>
          </dependency>
        </dependencies>
      </plugin>

      <plugin>
        <groupId>org.jacoco</groupId>
        <artifactId>jacoco-maven-plugin</artifactId>
        <version>${jacoco.version}</version>
        <configuration>
          <excludes>
            <!-- Yalnizca demo profilinde calisiyor, testlerde devrede degil.
                             Kapsam sayisini suslemesin diye disarida. -->
            <exclude>tr/ebrar/talep/config/DemoVeriYukleyici.class</exclude>
            <exclude>tr/ebrar/talep/TalepOnayApplication.class</exclude>
          </excludes>
        </configuration>
        <executions>
          <execution>
            <id>prepare-agent</id>
            <goals>
              <goal>prepare-agent</goal>
            </goals>
          </execution>
          <execution>
            <id>report</id>
            <goals>
              <goal>report</goal>
            </goals>
            <phase>verify</phase>
          </execution>
          <execution>
            <!-- Kapsam bir rapor degil kural. Esik altina duserse derleme kirilir,
                             boylece "sonra bakariz" diye birakilan test borcu birikmiyor. -->
            <id>kapsam-esigi</id>
            <goals>
              <goal>check</goal>
            </goals>
            <phase>verify</phase>
            <configuration>
              <rules>
                <rule>
                  <element>BUNDLE</element>
                  <limits>
                    <limit>
                      <counter>LINE</counter>
                      <value>COVEREDRATIO</value>
                      <minimum>0.80</minimum>
                    </limit>
                  </limits>
                </rule>
                <rule>
                  <element>PACKAGE</element>
                  <includes>
                    <include>tr.ebrar.talep.service</include>
                  </includes>
                  <limits>
                    <limit>
                      <counter>LINE</counter>
                      <value>COVEREDRATIO</value>
                      <minimum>0.85</minimum>
                    </limit>
                  </limits>
                </rule>
              </rules>
            </configuration>
          </execution>
        </executions>
      </plugin>

      <plugin>
        <groupId>com.diffplug.spotless</groupId>
        <artifactId>spotless-maven-plugin</artifactId>
        <version>${spotless.version}</version>
        <configuration>
          <!-- Bilincli olarak tam bicimlendirici (google-java-format gibi) yok:
                         var olan kodu bastan bicimlendirmek anlamsiz bir fark yigini uretir
                         ve gecmisi okunmaz hale getirir. Buradaki kurallar tartisma
                         yaratmayan, otomatik duzeltilebilen turden. -->
          <java>
            <includes>
              <include>src/main/java/**/*.java</include>
              <include>src/test/java/**/*.java</include>
            </includes>
            <removeUnusedImports/>
            <importOrder>
              <order>java,javax,jakarta,org,com,tr,</order>
            </importOrder>
            <trimTrailingWhitespace/>
            <endWithNewline/>
          </java>
          <pom>
            <sortPom>
              <expandEmptyElements>false</expandEmptyElements>
            </sortPom>
          </pom>
        </configuration>
        <executions>
          <execution>
            <!-- CI'da bicim ihlali derlemeyi kirar; yerelde ./mvnw spotless:apply duzeltir -->
            <id>bicim-denetimi</id>
            <goals>
              <goal>check</goal>
            </goals>
            <phase>validate</phase>
          </execution>
        </executions>
      </plugin>
    </plugins>
  </build>

  <profiles>
    <profile>
      <!--
        Mutasyon testi: ./mvnw -Pmutasyon test

        Neden ayri profil: pitest kodu kasitli olarak bozup testlerin fark edip
        etmedigine bakiyor, yani test paketini onlarca kez calistiriyor. Her
        derlemede kosturmak dakikalar ekler; ayda bir bakilacak bir olcum.

        Neden var: satir kapsami "bu satir calisti" der, "bu satir DOGRU calisti"
        demez. Bir metodun icini silsen bile testler yesil kalabilir. Mutasyon
        skoru testlerin gercekten bir sey iddia edip etmedigini olcuyor.

        Hedef yalnizca saf birim testleriyle kaplanan siniflar: veritabani ve
        Spring baglami gerektiren testler mutasyon kosusunda pratik degil.
      -->
      <id>mutasyon</id>
      <build>
        <plugins>
          <plugin>
            <groupId>org.pitest</groupId>
            <artifactId>pitest-maven</artifactId>
            <version>${pitest.version}</version>
            <configuration>
              <targetClasses>
                <param>tr.ebrar.talep.domain.*</param>
                <param>tr.ebrar.talep.service.TalepServisi</param>
              </targetClasses>
              <targetTests>
                <param>tr.ebrar.talep.domain.TalepDurumuTest</param>
                <param>tr.ebrar.talep.domain.VarlikKimlikTest</param>
                <param>tr.ebrar.talep.service.TalepServisiTest</param>
              </targetTests>
              <excludedMethods>
                <!-- toString yalnizca log ve hata ayiklama icin; davranis tasimiyor.
                     Getter'lar da ayni sekilde: mutasyonlari "test eksik" degil
                     "test edilecek bir sey yok" anlamina geliyor. -->
                <param>toString</param>
              </excludedMethods>
              <outputFormats>
                <param>HTML</param>
                <param>XML</param>
              </outputFormats>
              <timestampedReports>false</timestampedReports>
            </configuration>
            <dependencies>
              <dependency>
                <groupId>org.pitest</groupId>
                <artifactId>pitest-junit5-plugin</artifactId>
                <version>${pitest.junit5.version}</version>
              </dependency>
            </dependencies>
            <executions>
              <execution>
                <id>mutasyon-testi</id>
                <goals>
                  <goal>mutationCoverage</goal>
                </goals>
                <phase>test</phase>
              </execution>
            </executions>
          </plugin>
        </plugins>
      </build>
    </profile>
  </profiles>

</project>
